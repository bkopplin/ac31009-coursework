extends Node




"""
func authenticate(username, password) -> String:
	var user = get_user(username)
	if user == null:
		return "111"
	if user.password == password.sha256_text():
		return "110"
	return "111"

func create_account(options: Directory) -> String:
	if user_exists(options.username):
		return "101"
	
	var new_user: Directory
	new_user.username = options.username
	new_user.password = options.password.sha256_text()
	# player_database.append(new_user)
	return "100"
	"""
	

